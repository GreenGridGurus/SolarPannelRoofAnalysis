window.onload = function() {
    const header = document.querySelector('header[data-testid="stHeader"]');
    if (header) {
        header.style.backgroundImage = 'linear-gradient(to bottom, rgba(240, 243, 243, 0.9), rgba(240, 243, 243, 0))';
    }
}

